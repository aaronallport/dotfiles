#!/usr/bin/env bash

# My dependencies. Run as is, or copy each command as you see fit

# Install Homebrew
ruby -e "$(curl -fsSL https://raw.github.com/mxcl/homebrew/go)"

# Install syntax-highlighting for cat (use c <file>)
sudo easy_install Pygments
